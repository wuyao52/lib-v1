# Netlify 配置 - 直接使用预构建文件
[build]
  publish = "dist"

# SPA 路由重定向
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# API 代理重定向
[[redirects]]
  from = "/api/hongniaoai/*"
  to = "/.netlify/functions/proxy?path=hongniaoai/:splat"
  status = 200